<%- include('partials/head') %>

<body class="bg-gradient-to-br from-rose-50 via-white to-gray-100 min-h-screen flex flex-col">

  <!-- NAVBAR -->
  <%- include('partials/nav') %>

  <!-- MAIN -->
  <main class="flex-1 flex items-center justify-center px-6">
    <div class="relative bg-white shadow-2xl rounded-3xl p-12 w-full max-w-md overflow-hidden">

      <!-- Decorative Gradient Blobs -->
      <div class="absolute -top-24 -right-24 w-64 h-64 bg-rose-200 rounded-full blur-3xl opacity-40"></div>
      <div class="absolute -bottom-24 -left-24 w-64 h-64 bg-gray-200 rounded-full blur-3xl opacity-40"></div>

      <!-- Heading -->
      <h2 class="text-4xl font-extrabold text-gray-800 mb-3 text-center">
        Register your home
      </h2>

      <p class="text-gray-500 text-center mb-10 text-lg">
        Share your place with travelers around the world 🌍
      </p>

      <!-- FORM -->
      <form action="/host/add-home" method="POST" class="space-y-6 relative z-10">

        <!-- Input -->
        <div>
          <label class="block text-gray-700 font-medium mb-2">
            Home Name
          </label>
          <input
            type="text"
            name="houseName"
            placeholder="e.g. Cozy Beach House"
            required
            class="w-full px-5 py-3 border border-gray-300 rounded-xl
                   focus:outline-none focus:ring-2 focus:ring-rose-500
                   focus:border-rose-500 transition"
          />
        </div>

        <!-- Submit -->
        <button
          type="submit"
          class="w-full bg-rose-500 hover:bg-rose-600 text-white py-4
                 rounded-xl font-semibold text-lg
                 transition-all duration-300 shadow-lg hover:shadow-xl"
        >
          🏠 Add Home
        </button>

      </form>

      <!-- Back -->
      <div class="text-center mt-8 relative z-10">
        <a href="/" class="text-sm text-gray-500 hover:text-rose-500 transition">
          ← Back to Home
        </a>
      </div>

    </div>
  </main>

  <!-- FOOTER -->
  <footer class="text-center py-4 text-gray-400 text-sm">
    © 2025 Airbnb Clone • Host with confidence
  </footer>

</body>
</html>
