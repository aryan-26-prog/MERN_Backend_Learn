//Event Loop = The event loop is the core mechanism that allows Node.js to perform non-blocking I/O operations and handle concurrent requests with a single thread.

//Event loop is single threaded which can use all the functions.

//V8 engine = open source js engine by google and compiles js to native machines.

//libuv = It is a multi platform support library for Node.js. It can also handle asynchronous I/O operations.

// Asynchronous programming = Asynchronous programming in Node.js is a non-blocking execution model that allows the program to perform multiple tasks concurrently without waiting for slow operations (like file I/O or network requests) to complete.
